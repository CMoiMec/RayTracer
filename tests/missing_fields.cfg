# === Missing fields ===
cameras = (
    {
        name = "main_camera";
        resolution = { width = 800; height = 600; };
        position = { x = 0; y = 0; z = 5; };
    }
);

# === Primitives dans la scène ===
primitives = {
    spheres = (
        { x = 0; y = 0; z = -10; r = 1; }
    );

    planes = (
        { axis = "Y"; color = { r = 200; g = 200; b = 200; }; position = 0; }
        # La position est spécifiée ici car elle semble être obligatoire
    );
};

# === Configuration des lumières avec champs manquants ===
lights = {
    ambient = 0.0;
    diffuse = 0.0;

    point = ();

    directional = ();
};
